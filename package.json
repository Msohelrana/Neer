{
  "name": "neer-chat",
  "version": "1.0.0",
  "description": "A static, vanilla-JS 1-on-1 chat app backed by Firebase.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
